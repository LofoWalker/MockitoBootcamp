package com.lofo.learnMockito.unitaire.training;

/**
 * We will now test a controller.
 * This kind of test requires more knowledge of the tests and in particular of Mockito.
 * Try here to follow the development steps without looking at the correction
 */
public class TodoControllerTest {

    /**
     * First, declare the variables necessary for writing the tests.
     * To know them, go directly to the controller.
     */

    /**
     * Initialize your tests
     */
    public void setup() {
    }

    /**
     * Check that the addition of an objettodo works. We are here in a unit test context.
     * We will have to mock the base.
     */
    public void should_add_a_todo_and_return_him() {
    }

    /**
     * Check that the recovery of base todos is working properly.
     */
    public void should_get_all_todo_and_return_them() {
    }
}
